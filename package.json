{
  "name": "sauna-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.2",
    "ffmpeg-static": "^5.3.0",
    "fluent-ffmpeg": "^2.1.3",
    "sharp": "^0.34.5",
    "vite": "^8.0.10",
    "wrangler": "^4.88.0"
  },
  "dependencies": {
    "lottie-web": "^5.13.0",
    "three": "^0.184.0"
  }
}
